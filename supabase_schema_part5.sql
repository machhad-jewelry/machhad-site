-- إضافة: صورة شخصية للزبون (تُخزَّن Base64 بنفس أسلوب صور المنتجات بهاي القاعدة —
-- لا يوجد Storage bucket بالمشروع حاليًا، فاستُخدم نفس النمط الموجود بدل إدخال بنية جديدة)
-- (لا تحذف أي جدول قديم — هذه إضافة فوق الجداول الموجودة)

alter table customers add column if not exists avatar_url text;
